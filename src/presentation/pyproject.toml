[project]
name = "presentation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "RyotaMiyatsuka", email = "miya882308@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.12",
    "infrastructure",
    "pydantic>=2.10.6",
    "use-case",
    "uvicorn>=0.34.0",
]

[project.scripts]
presentation = "presentation:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
use-case = { workspace = true }
infrastructure = { workspace = true }
